import React, { Components } from "react";

const Footer = () => {
  return (
    <footer className="footer mt-auto py-3">
      <div className="container">
        <span className="text-muted">Appplication in development phase</span>
      </div>
    </footer>
  );
};

export default Footer;
